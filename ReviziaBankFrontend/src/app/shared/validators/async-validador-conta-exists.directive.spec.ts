import { AsyncValidadorContaExistsDirective } from './async-validador-conta-exists.directive';

describe('AsyncValidadorContaExistsDirective', () => {
  it('should create an instance', () => {
    const directive = new AsyncValidadorContaExistsDirective();
    expect(directive).toBeTruthy();
  });
});
